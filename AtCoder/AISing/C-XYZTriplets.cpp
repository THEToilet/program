#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
typedef long long ll;
using namespace std;
int F(int i)
{
  int ans = 0;
  

  return ans;
}
int main()
{
  int N;
  cin >> N;
  // x^2+y^2+z^2+2xy+2yz+2zx
  int x, y, z;
  pow((x + y + z), 2);
  for (int i = 1; i <= N; i++)
  {
    cout << F(i) << endl;
  }
  return 0;
}