#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
typedef long long ll;
using namespace std;
int main()
{
  ll N;
  cin >> N;
  vector<ll> A(N);
  rep(i, N)
  {
    cin >> A[i];
  }
  return 0;
}