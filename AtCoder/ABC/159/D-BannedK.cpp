#include <bits/stdc++.h>
using namespace std;
int main()
{
  long int N;
  vector<long int> A;
  cin >> N;
  for (int i = 0; i < N; i++)
  {
    long int a;
    cin >> a;
    A.push_back(a);
  }
  for (int i = 0; i < N; i++)
  {
    for (int k = 0; k < N; k++)
    {
      A.at(i);
    }
  }
}