#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
typedef long long ll;
using namespace std;
int main()
{
  int N, M, Q;
  cin >> N >> M >> Q;
  vector<int> a(Q), b(Q), c(Q), d(Q);
  vector<int> A(M);
  rep(i, Q)
  {
    cin >> a[i] >> b[i] >> c[i] >> d[i];
  }
  rep(i,M){
    
  }
  /*
  for (int i = 1; i <= M; i++)
  {
    for (int j = i + 1; j <= M; j++)
    {
      for (int k = j + 1; j <= M; j++)
      {
        rep(i,M){
          A[i]
        }
      }
    }
  }
  /*
  return 0;
}